const getSources = (ev, data) => {}

const addSource = (ev, data) => {}

const updateSource = (ev, data) => {}

const deleteSource = (ev, data) => {}

module.exports = {
  getSources,
  addSource,
  updateSource,
  deleteSource,
}
